import 'package:flutter/material.dart';

class AssignedByMe extends StatelessWidget {
  const AssignedByMe({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
